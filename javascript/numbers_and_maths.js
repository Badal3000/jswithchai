console.log(Math)
//llibraries used in maths
//Math.abs()->for the absolute values
console.log(Math.abs(-10))
console.log(Math.round(100.5)) // used for round off the value
console.log(Math.ceil(100.01)) //used for the ceiling value(higher value)
console.log(Math.floor(100.99)) // used for the floor value(lower value)
console.log(Math.min(8, 9 , 3, 4)) // calculates the minimmum value
console.log(Math.max(4,9,5,3,1,)) // calcullates the maximum value
console.log(Math.random()) // gives the random value between 0 to onr but we can manipulate it within the ranges by multiplying between the values of multiples of 10  and applying floor value so it doesnt go out of bound
console.log(Math.random()*100) // gives the random value between 0 to onr but we can manipulate it within the ranges by multiplying between the values of multiples of 10  and applying floor value so it doesnt go out of bound
console.log(Math.floor(Math.random()*100)) // gives the random value between 0 to onr but we can manipulate it within the ranges by multiplying between the values of multiples of 10  and applying floor value so it doesnt go out of bound